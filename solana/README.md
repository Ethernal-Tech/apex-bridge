# Solana integration

## How to download Solana CLI

Copy this into your terminal and follow the steps provided by the installation

```bash
curl --proto '=https' --tlsv1.2 -sSfL https://solana-install.solana.workers.dev | bash
```

### Verify Solana CLI installation

Paste this into your terminal:

```bash
rustc --version && solana --version && anchor --version && surfpool --version && node --version && yarn --version

```

## How to run tests

Copy program keypair & one fee payer keypair in tests folder
